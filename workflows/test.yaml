name: Reusable workflow example

on:
  workflow_call:

jobs:
  example_job:
    name: Reusable workflow example
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        shell: bash
        run: echo hello world
